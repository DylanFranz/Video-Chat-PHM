<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script>
        const ROOM_ID = "<%= roomId %>";
    </script>
    <script src="https://unpkg.com/peerjs@1.5.4/dist/peerjs.min.js" defer></script>
    <script src="/socket.io/socket.io.js" defer></script>
    <script src="script.js" defer></script>
    <script src="https://kit.fontawesome.com/728205072d.js" crossorigin="anonymous"></script>
    <title>Video Call</title>
    <style>
        body {
            background-color:darkgray;
            color: rgb(0, 0, 0);
        }
        
        #video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, 300px);
            grid-auto-rows: 300px;
            grid-gap: 25px;
            padding: 1em;
        }

        #round-button {
            height: 5rem;
            width: 5rem;
            border-radius: 50%;
            border: 1px solid black;
        }
        
        #icon {
            font-size:xx-large;
        }

        video {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 25px;
            border-style: solid;
            border-color: black;
            border-width: 10px;
        }
    </style>
</head>
<body>
    <div id="video-grid"></div>
    <div id="bottom-bar" style="background-color: blanchedalmond; justify-content:space-between; padding: 1em; bottom: 1em; position: fixed; width:max-content">
        <button onclick="location.href = '/logout';" id="round-button"><i class="fa-solid fa-phone-slash" id="icon"></i></button>
    </div>
</body>
</html>